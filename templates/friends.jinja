<!-- 
-->
{% extends 'base.jinja' %}

{% block content %} 
   

<main>
    <p> {{ username }}'s Friends: </p>
    <ul id="list"></ul>
    <script type="text/javascript">
        let list = document.getElementById('list');
        for (let i = 0; i < {{ friends }}.length; i++) {
            let li = document.createElement('li');
            li.textContent = {{ friends }}[i];
            list.appendChild(li);
        }
    </script>

    <h2>Friend Requests</h2>
    <ul id="friend-requests">
        <!-- Display pending friend requests here -->
        {% for request in friend_requests %}
            <li>{{ request.sender }} sent you a friend request 
                <button onclick="acceptFriendRequest('{{ request.sender }}')">Accept</button>
                <button onclick="declineFriendRequest('{{ request.sender }}')">Decline</button>
            </li>
        {% endfor %}
    </ul>

    <script type="text/javascript">
        // Function to handle accepting friend requests
        function acceptFriendRequest(sender) {
            // Logic to accept the friend request
            // You can implement this as per your friend request handling
        }

        // Function to handle declining friend requests
        function declineFriendRequest(sender) {
            // Logic to decline the friend request
            // You can implement this as per your friend request handling
        }
    </script>

</main>


{% endblock %}
